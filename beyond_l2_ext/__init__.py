"""Plugin package providing the CaBRNet architecture/similarity extensions needed to
reproduce the "Beyond L2" paper's experiments, on top of the public `cabrnet` package.

These classes are loaded dynamically by CaBRNet's config system (`module`/`name` fields
in YAML configs) and are not meant to be imported directly in most cases.
"""
